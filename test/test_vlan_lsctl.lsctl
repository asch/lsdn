settings vlan -nets {42 43}

phys -if out -name a -nets {42 43}
phys -if out -name b -nets {42 43}

net 42 {
    virt -if 1 -phys a
    virt -if 2 -phys a
    virt -if 1 -phys b
}

net 43 {
    virt -if 3 -phys a
    virt -if 2 -phys b
}

claimLocal $localPhys

commit
